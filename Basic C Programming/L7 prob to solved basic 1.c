#include<stdio.h>
int dividing(int a, int b)
{
   float result = a/b;
}
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    result = dividing(a,b);
    printf("Result = %f\n",(float)result);
}
